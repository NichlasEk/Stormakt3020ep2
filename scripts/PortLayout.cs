using System.Numerics;
namespace Atland;

public static class PortLayout
{
    // Retained from 0.4.1 so the optional find and existing saves remain compatible.
    public static readonly Vector2 Cache=new(361.6f,570.4f);
}
